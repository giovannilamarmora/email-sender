spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: email@email.com
    password: token-id
    sender-emails: email@email.com
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

rest:
  client:
    strapi:
      active: true
      baseUrl: https://strapi.giovannilamarmora.com
      bearer: token
      path:
        getTemplate: /api/email-templates